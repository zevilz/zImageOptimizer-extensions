# Extension: wp_change_ext
# Hook: init-loop-vars-after

IMAGES=$(echo "$IMAGES" | sort -r)

#if [[ "$OSTYPE" == "linux-gnu" ]]; then
#	IMAGES=$(echo "$IMAGES" | tac)
#elif [[ "$OSTYPE" == "darwin"* ]]; then
#	IMAGES=$(echo "$IMAGES" | gtac)
#elif [[ "$OSTYPE" == "FreeBSD"* ]]; then
#	IMAGES=$(echo "$IMAGES" | sed 'x;1!H;$!d;x')
#fi

#IMAGES=$(echo "$IMAGES" | perl -e 'print reverse <>')
